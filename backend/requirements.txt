# ===========================================
# MiroFish Lite — Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ── Core Framework ──────────────────────────────────────────
flask>=3.0.0
flask-cors>=6.0.0

# ── LLM Providers ──────────────────────────────────────────
google-genai>=1.2.0  # Used in current GeminiService
groq>=0.11.0 

# ── Memory: Supabase (PostgreSQL + pgvector) ─────────────────
supabase>=2.0.0

# ── OASIS Social Simulation Engine ──────────────────────────
# Kept for simulation scripts; routed to Gemini via OpenAI-compat endpoint
openai>=1.0.0
camel-oasis==0.2.5
camel-ai==0.2.78

# ── File Processing ──────────────────────────────────────────
PyMuPDF>=1.24.0
charset-normalizer>=3.0.0
chardet>=5.0.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
